% vim: set foldmethod=marker foldlevel=0:

\documentclass[a4paper]{article}
\usepackage[UKenglish]{babel}

\usepackage{preamble}

\fancyhead[L]{MA260 Assignment 4}
\title{MA260 Norms Metrics and Topologies, Assignment 4}
\colorlet{questionbodycolor}{orange!50!violet}

\begin{document}

\maketitle

\setlength{\parindent}{0em}
\setlength{\parskip}{1em}

% {{{ Q1
\question{1}

\begin{questionbody}
Let $X$ be a topological space and $A \subset X$. Suppose that for each $x \in A$ there is an open set $U$ containing $x$ such that $U \subset A$. Show that $A$ is open in $X$.
\end{questionbody}

Answer

% }}}

% {{{ Q2
\newquestion{2}

\begin{questionbody}
Let $f : S^1 \to \R$ be a continuous map. Show that there exists a point $x$ of $S^1$ such that $f(x) = f(-x)$. Here $S^1$ is the unit circle in $\R^2$ and $-x$ is the antipode of $x$.
\end{questionbody}

Answer

% }}}

% {{{ Q3
\newquestion{3}

\begin{questionbody}
\begin{enumerate}[(a)]
\item Show that the product of two path connected spaces is path connected.

\item If $f : X \to Y$ is continuous and $X$ is path connected, is $f(X)$ necessarily path connected? Justify your answer.
\end{enumerate}
\end{questionbody}

\subsection{~} % 3.a

Answer

\subsection{~} % 3.b

Answer

% }}}

% {{{ Q4
\newquestion{4}

\begin{questionbody}
Let $X$ be compact and $Y$ be Hausdorff. Show that if $f : X \to Y$ is continuous then $f$ is a closed map, so the image of every closed set is closed.
\end{questionbody}

Answer

% }}}

% {{{ Q5
\newquestion{5}

\begin{questionbody}
Show that if $Y$ is compact then the projection $\pi_1 : X \times Y \to X$ is a closed map, so the image of every closed set is closed.
\end{questionbody}

Answer

% }}}

\end{document}
