%%%%% Default packages

\usepackage{amsmath, amssymb, amsgen}
\usepackage{gensymb}
\usepackage{mathtools}
\usepackage{csquotes}
\usepackage{cancel}
\usepackage[shortlabels]{enumitem}

%%%%% Nice headers and title stuff

\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhead[R]{Dyson Dyson 5503449}
\fancyhead[C]{Question~\arabic{section}}

\author{Dyson Dyson}
\date{}

%%%%% Make question numbers seamless

\renewcommand{\thesection}{Question~\arabic{section}}
\renewcommand{\thesubsection}{Q\arabic{section}~(\alph{subsection})}
\renewcommand{\thesubsubsection}{Q\arabic{section}~(\alph{subsection}) \roman{subsubsection})}

\newcommand{\question}[1]{%
	\setcounter{section}{#1}
	\addtocounter{section}{-1}
	\section{}
	\setcounter{subsection}{0}
}

\newcommand{\newquestion}[1]{%
	\newpage
	\question{#1}
}

%%%%% Quote question bodies

\usepackage{mdframed}
\usepackage{xcolor}

% Deliberately looks bad to remind me to set color for each module
% Should be overriden in every file
\colorlet{questionbodycolor}{black}

\newmdenv[
	linewidth=3pt,
	hidealllines=true,
	leftline=true,
	innerleftmargin=0.5em,
	innerrightmargin=0.5em,
	linecolor=questionbodycolor,
	backgroundcolor=questionbodycolor!25!white,
]{questionbody}

%%%%% Include code snippets

\usepackage{minted}
\setminted{
	linenos,
	breaklines,
	fontsize=\footnotesize,
	encoding=utf8,
	style=catppuccin-latte,
}
\usepackage[quiet]{fontspec}
\setmonofont[Scale=MatchLowercase]{Hack}

%%%%% Shorthand and missing functions (taken mostly from my Obsidian notes)
% Environments
\newenvironment{apmatrix}[2]
	{\left(\begin{array}{ @{} *{#1}{c} | *{#2}{c} @{} }}
	{\end{array}\right)}

% Easy inline math operator
\newcommand{\op}[1]{\mathop{\rm #1}}

% Enumerate set
\newcommand{\enum}[3]{{#1}_{#2},\ \ldots,\ {#1}_{#3}}

% Angle brackets
\newcommand{\angb}[1]{\left\langle #1 \right\rangle}

% Calculus stuff
\renewcommand{\d}{\operatorname{d}\!}
\newcommand{\diff}[1]{\frac{\d\,}{\d #1}}
\newcommand{\dd}[2]{\frac{\d #1}{\d #2}}
\newcommand{\ddd}[3]{\frac{\d^{#1} #2}{\d {#3}^{#1}}}
\newcommand{\pdiff}[1]{\frac{\partial\,}{\partial #1}}
\newcommand{\pdd}[2]{\frac{\partial #1}{\partial #2}}
\newcommand{\pddd}[3]{\frac{\partial^{#1} #2}{\partial {#3}^{#1}}}
\newcommand{\inte}[2]{\int #1 \,\d #2}
\newcommand{\intlim}[4]{\int_{#1}^{#2} #3 \,\d #4}
\newcommand{\llim}[1]{\lim\limits_{#1}}

% Sets
\newcommand{\N}{\mathbb{N}}
\newcommand{\Z}{\mathbb{Z}}
\newcommand{\Q}{\mathbb{Q}}
\newcommand{\R}{\mathbb{R}}
\newcommand{\C}{\mathbb{C}}
\renewcommand{\H}{\mathbb{H}}

% Font commands
\newcommand{\bb}{\mathbb}
\renewcommand{\cal}{\mathcal}

% Misc shortened commands
\renewcommand{\l}{\left}
\newcommand{\m}{\middle}
\renewcommand{\r}{\right}
\newcommand{\ds}{\displaystyle}
\newcommand{\ts}{\textstyle}
\newcommand{\f}{\frac}
\newcommand{\df}{\dfrac}
\newcommand{\thf}{\therefore\ }
\newcommand{\fa}{\forall\ }
\newcommand{\qqt}[1]{\quad\quad\text{#1}}
\newcommand{\ul}{\underline}
\newcommand{\smlm}{\sum\limits}
\newcommand{\rref}{\text{RREF}}

% Other letters
\newcommand{\e}{\mathrm{e}}

% Trig functions
\def\cosec{\mathop{\rm cosec}\nolimits}
\def\sech{\mathop{\rm sech}\nolimits}
\def\cosech{\mathop{\rm cosech}\nolimits}
\def\arcsin{\mathop{\rm arcsin}\nolimits}
\def\arccos{\mathop{\rm arccos}\nolimits}
\def\arctan{\mathop{\rm arctan}\nolimits}
\def\arsinh{\mathop{\rm arsinh}\nolimits}
\def\arcosh{\mathop{\rm arcosh}\nolimits}
\def\artanh{\mathop{\rm artanh}\nolimits}

% Text functions
\def\Re{\mathop{\rm Re}\nolimits}
\def\Im{\mathop{\rm Im}\nolimits}
\def\Mat{\mathop{\rm Mat}}
\def\lcm{\mathop{\rm lcm}\nolimits}
\def\grad{\mathop{\rm grad}\nolimits}
